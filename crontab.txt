# Japanese Learning Pipeline — daily cron for dmit-hk Docker setup
#
# Install on dmit-hk (runs pipeline inside the web container at 03:00 HKT / 19:00 UTC):
#   crontab -e
#   (paste the line below, save)
#
# Or non-interactively:
#   (crontab -l 2>/dev/null; cat /root/mimichan/crontab.txt | grep -v '^#' | grep -v '^$') | crontab -
#
# Verify: crontab -l

# ┌── minute (0)
# │  ┌── hour (19 UTC = 03:00 HKT)
# │  │  ┌── day of month (*)
# │  │  │  ┌── month (*)
# │  │  │  │  ┌── day of week (*)
# │  │  │  │  │
  0  19  *  *  *  docker exec mimichan-web-1 python pipeline.py >> /root/mimichan/pipeline.log 2>&1
